package model.entity.being.ants;

/**
 * Enum representing different types of ants in the game.
 * Used primarily for saving/loading and rendering.
 */
public enum AntType {
    WORKER_ANT,
    QUEEN,
    LARVA
}
