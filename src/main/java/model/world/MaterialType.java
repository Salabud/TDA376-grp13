package model.world;

/**
 * Enum representing different types of materials for tiles/items in the world.
 */
public enum MaterialType {
    DIRT,
    STONE,
    FOOD,
    GRASS,
    CORPSE,
    POISON
}
